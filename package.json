{
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=7.0.0"
  },
  "type": "module",
  "name": "smileyBall",
  "scripts": {
    "build": "exit 0",
    "prebuild": "exit 0",
    "pretest": "exit 0",
    "start": "exit 0"
  },
  "dependencies": {
    "@dfinity/agent": "^1.4.0",
    "@dfinity/auth-client": "^1.4.0"
  }
}